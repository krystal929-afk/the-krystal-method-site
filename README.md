# The Krystal Method — Website Package v1

Open `index.html` in a browser to preview.

## Fast Manus instructions
1. Upload this whole folder or ZIP to Manus.
2. Tell Manus: "Use `index.html` and `styles.css` as the source of truth. Do not redesign. Preserve the dark editorial The Krystal Method brand, Cormorant Garamond + Oswald typography, toxic lime/purple accents, and the current copy. Fix only broken image paths if needed and publish as a static landing page."
3. If Manus asks what to deploy, deploy `index.html` from the root folder.

## Folder map
- `index.html` — complete homepage
- `styles.css` — full responsive CSS
- `assets/logos` — The Krystal Method logo system
- `assets/featured` — curated portfolio images used on the page
- `assets/library` — extra PNG/JPG assets from the Landing Page ZIP
- `docs` — reference PDFs only; not required for the live homepage

## Do not spend credits on
- Rewriting all copy
- Rebranding
- Starting from scratch
- Building multiple pages tonight

## Good next prompt for Manus
"Deploy this as a static portfolio homepage. Keep the existing HTML/CSS. Make sure all local image paths resolve. Do not redesign. Optimize only for mobile responsiveness and image loading."
